#include "stdlib.h"
#include "stdio.h"
#include "unistd.h"
#include "time.h"


int main(){
    int h=20;
    int m=59;
    int s=58;
    char* M= malloc(2), * S= malloc(2);



    /*while(1) {
        s=(s+1)%60;
        m+=((s==0));
        m%=60;
        h+=(m==0)&&(s==0);
        printf("%d : %d : %d\n",h,m,s);

        sleep(1);

    }
*/
    return 0;
};

